#include "Frontier.h"

using namespace std;

//template void Frontier<greater<CostlySnakePath>>::pushIfUnique(CostlySnakePath&&);
//template void Frontier<greater<CostlySnakePath>>::pushIfUnique(CostlySnakePath&);
//
//template void Frontier<less<CostlySnakePath>>::pushIfUnique(CostlySnakePath&&);
//template void Frontier<less<CostlySnakePath>>::pushIfUnique(CostlySnakePath&);
